apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-deployment
  labels:
    app: worker-main
spec:
  replicas: 2
  selector:
    matchLabels:
      app: worker-main
  template:
    metadata:
      labels:
        app: worker-main
    spec:
      containers:
      - name: api-container
        image: nitinxyz/poc-worker:latest
        imagePullPolicy: Always
        env:
          - name: REDIS_HOST
            value: redis-service
