services:
  jenkins:
    build: ./jenkins
    ports:
      - "9090:8080"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - default
      - kind
volumes:
  jenkins_home:
networks:
  kind:
    external: true
